// Package store is reserved for the SQLite-backed persistence layer
// added in P1-2. It currently has no exported symbols; placeholder
// only so Git tracks the directory and downstream tickets have a
// stable home for their imports.
package store
